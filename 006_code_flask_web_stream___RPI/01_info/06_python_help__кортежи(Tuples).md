Кортежи (Tuples) в Python — это неизменяемые упорядоченные коллекции элементов.
Основные характеристики:
1. Создание кортежа

# Круглые скобки
tuple1 = (1, 2, 3, 4, 5)

# Можно без скобок (но это менее читаемо)
tuple2 = 1, 2, 3

# С одним элементом (обязательно запятая!)
single_tuple = (42,)  # кортеж
not_a_tuple = (42)    # просто число 42

# Пустой кортеж
empty_tuple = ()

2. Неизменяемость (immutable)
python

my_tuple = (1, 2, 3)
# my_tuple[0] = 10  # ОШИБКА! Кортеж нельзя изменить

# Но можно создать новый кортеж
new_tuple = my_tuple + (4, 5)  # (1, 2, 3, 4, 5)

3. Доступ к элементам
python

colors = ("красный", "зеленый", "синий")

# По индексу
first_color = colors[0]  # "красный"
last_color = colors[-1]  # "синий"

# Срезы (slices)
two_colors = colors[1:3]  # ("зеленый", "синий")

4. Использование в словарях как ключи
python

# Кортежи можно использовать как ключи в словарях
coordinates_dict = {
    (0, 0): "начало координат",
    (1, 2): "точка A",
    (3, 4): "точка B"
}

print(coordinates_dict[(1, 2)])  # "точка A"

# Списки НЕЛЬЗЯ использовать как ключи!
# bad_dict = {[1, 2]: "значение"}  # ОШИБКА!

5. Распаковка кортежа (unpacking)
python

person = ("Анна", 25, "Москва")
name, age, city = person

print(name)  # "Анна"
print(age)   # 25
print(city)  # "Москва"

# Обмен значений (без временной переменной)
a, b = 10, 20
a, b = b, a  # a = 20, b = 10

6. Методы кортежа
python

my_tuple = (1, 2, 2, 3, 4, 2)

# count() - подсчет элементов
count_twos = my_tuple.count(2)  # 3

# index() - поиск индекса элемента
index_of_3 = my_tuple.index(3)  # 3

7. Когда использовать кортежи вместо списков?
python

# 📍 Кортеж лучше, когда:
# 1. Данные не должны меняться
coordinates = (55.7558, 37.6173)  # широта, долгота Москвы

# 2. Используется как ключ в словаре
graph_points = {
    (0, 0): "A",
    (1, 1): "B"
}

# 3. Возврат нескольких значений из функции
def get_user_info():
    return "Анна", 25, "инженер"  # автоматически упаковывается в кортеж

name, age, job = get_user_info()

# 4. Фиксированный набор параметров
RGB_COLORS = (255, 0, 0)  # красный цвет

# 📍 Список лучше, когда:
# 1. Нужно изменять данные
shopping_list = ["молоко", "хлеб"]
shopping_list.append("яйца")

# 2. Однородные данные
numbers = [1, 2, 3, 4, 5]

8. Преимущества кортежей

    Безопасность: данные защищены от случайного изменения

    Скорость: работают быстрее списков

    Память: занимают меньше памяти

    Хешируемость: можно использовать как ключи в словарях

9. Примеры на практике
python

# Координаты
point = (10, 20)
x, y = point

# Дата/время как структура
event = ("2024-01-15", "10:30", "Совещание")
date, time, description = event

# Настройки в конфигурации
database_config = ("localhost", 5432, "my_database", "user", "password")

# Возврат нескольких значений
def min_max(numbers):
    return min(numbers), max(numbers)

minimum, maximum = min_max([1, 5, 3, 9, 2])

10. Сравнение кортежа и списка
python

# Создание
tuple_example = (1, 2, 3)   # кортеж
list_example = [1, 2, 3]    # список

# Изменение
# tuple_example[0] = 10     # ❌ Ошибка
list_example[0] = 10        # ✅ OK [10, 2, 3]

# Добавление
# tuple_example.append(4)   # ❌ Нет метода append
list_example.append(4)      # ✅ OK [10, 2, 3, 4]

# Использование как ключ
dict_with_tuple = {tuple_example: "значение"}  # ✅ OK
# dict_with_list = {list_example: "значение"}  # ❌ Ошибка

Кортежи — это идеальный выбор для фиксированных наборов данных, которые логически связаны и не должны изменяться в процессе выполнения программы.

# ======================================
# ======================================
# ======= Отличие от списков # =========
# ======================================

1. Изменяемость (самое важное!)
python

# 📍 СПИСОК - ИЗМЕНЯЕМЫЙ (mutable)
my_list = [1, 2, 3]
my_list[0] = 10       # ✅ МОЖНО изменить
my_list.append(4)     # ✅ МОЖНО добавить
my_list.remove(2)     # ✅ МОЖНО удалить
print(my_list)        # [10, 3, 4]

# 📍 КОРТЕЖ - НЕИЗМЕНЯЕМЫЙ (immutable)
my_tuple = (1, 2, 3)
# my_tuple[0] = 10    # ❌ НЕЛЬЗЯ изменить - ОШИБКА!
# my_tuple.append(4)  # ❌ Нет такого метода - ОШИБКА!
# del my_tuple[0]     # ❌ НЕЛЬЗЯ удалить - ОШИБКА!

2. Синтаксис (скобки)

list_example = [1, 2, 3]     # квадратные скобки
tuple_example = (1, 2, 3)    # круглые скобки
tuple_example2 = 1, 2, 3     # или без скобок (но это сбивает с толку)

3. Использование как ключи словаря

# ✅ КОРТЕЖ - можно
coordinates = {(0, 0): "начало", (1, 2): "точка A"}

# ❌ СПИСОК - нельзя
# error = {[0, 0]: "начало"}  # TypeError: unhashable type: 'list'

4. Производительность

import sys
import timeit

my_list = [1, 2, 3, 4, 5]
my_tuple = (1, 2, 3, 4, 5)

# 📍 Память
print(sys.getsizeof(my_list))   # 104 байта (обычно больше)
print(sys.getsizeof(my_tuple))  # 88 байта (обычно меньше)

# 📍 Скорость создания
list_time = timeit.timeit('[1, 2, 3, 4, 5]', number=1000000)
tuple_time = timeit.timeit('(1, 2, 3, 4, 5)', number=1000000)
# Кортежи создаются быстрее

5. Методы (функциональность)

# 📍 СПИСОК - много методов для изменений
my_list = [1, 2, 3]
my_list.append(4)        # [1, 2, 3, 4]
my_list.extend([5, 6])   # [1, 2, 3, 4, 5, 6]
my_list.insert(0, 0)     # [0, 1, 2, 3, 4, 5, 6]
my_list.pop()            # удаляет 6
my_list.sort()           # сортировка
my_list.reverse()        # разворот

# 📍 КОРТЕЖ - только 2 метода
my_tuple = (1, 2, 2, 3)
count = my_tuple.count(2)  # 2
index = my_tuple.index(3)  # 3
# Больше ничего нельзя!

6. Когда что использовать (практические примеры)
#### 📍 Кортеж используй, когда: ##=========================

# 1. Константные данные
COLORS = ("красный", "зеленый", "синий")  # цвета светофора
PI = (3, 14)  # приближение числа π

# 2. Координаты, точки
point = (10, 20)  # x=10, y=20
latitude, longitude = (55.7558, 37.6173)  # Москва

# 3. Запись в БД (фиксированная структура)
user_record = ("Анна", 25, "anna@mail.ru")

# 4. Ключи в словарях
cache = {}
cache[(1, 2)] = "результат вычисления"

# 5. Возврат нескольких значений из функции
def get_stats(data):
    return min(data), max(data), sum(data)/len(data)

#### 📍 Список используй, когда: ##=========================

# 1. Динамические коллекции
shopping_list = ["молоко", "хлеб"]
shopping_list.append("яйца")
shopping_list.remove("хлеб")

# 2. Стек или очередь
stack = []
stack.append(1)  # push
item = stack.pop()  # pop

# 3. Изменяемые последовательности
numbers = [1, 2, 3]
numbers[0] = 10
numbers.sort()

# 4. Когда нужно менять порядок
tasks = ["задача1", "задача2", "задача3"]
tasks.reverse()  # ["задача3", "задача2", "задача1"]

7. "Защита" данных
python

# Кортеж защищает от случайных изменений
def process_data(data):
    # data - кортеж, можно быть уверенным, что он не изменился
    return len(data)

# Со списком нужно быть осторожным
def bad_function(some_list):
    some_list.append("ошибка!")  # изменил исходные данные
    return len(some_list)

my_list = [1, 2, 3]
result = bad_function(my_list)
print(my_list)  # [1, 2, 3, "ошибка!"] - список изменился!

8. Сравнение в таблице
Критерий	Список	Кортеж
Изменяемость	✅ Изменяемый	❌ Неизменяемый
Синтаксис	[ ]	( ) или без скобок
Скорость	Медленнее	Быстрее
Память	Больше	Меньше
Ключ в словаре	❌ Нельзя	✅ Можно
Методы	Много (append, remove, sort, etc)	Только count() и index()
Использование	Динамические данные	Фиксированные данные
Безопасность	Можно случайно изменить	Защита от изменений
9. Можно конвертировать друг в друга

# Список → Кортеж
my_list = [1, 2, 3]
my_tuple = tuple(my_list)  # (1, 2, 3)

# Кортеж → Список
my_tuple = (1, 2, 3)
my_list = list(my_tuple)   # [1, 2, 3]

10. Вывод
Используй списки, когда:
    Нужно добавлять/удалять элементы
    Данные будут меняться
    Нужна сортировка или изменение порядка

Используй кортежи, когда:
    Данные фиксированы и не должны меняться
    Нужно использовать как ключ в словаре
    Хочешь защитить данные от случайных изменений
    Возвращаешь несколько значений из функции
    Важна производительность (оптимизация)

