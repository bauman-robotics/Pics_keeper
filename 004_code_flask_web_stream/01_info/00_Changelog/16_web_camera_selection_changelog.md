# Changelog - Выбор камер на веб-странице

## Версия с выбором камер на веб-странице (2026-01-31)

### 📝 Новые возможности

#### Добавление веб-интерфейса для выбора камер
- **Модальное окно камер**: Визуальный интерфейс для просмотра и выбора доступных камер
- **Информация о камерах**: Отображение названия, устройства, форматов и разрешений
- **Визуальное выделение**: Текущая камера и выбранная камера выделяются цветом
- **API для камер**: REST API для получения списка камер и выбора камеры

### 🔧 Технические улучшения

#### Новые API маршруты:
```python
@app.route('/api/cameras')
def get_cameras():
    """Получение списка доступных камер"""

@app.route('/api/cameras/select', methods=['POST'])
def select_camera():
    """Выбор камеры для стрима"""
```

#### Новые функции в JavaScript:
```javascript
async function showCameras() {
    // Отображение модального окна с камерами
}

function renderCamerasList(data) {
    // Рендеринг списка камер
}

async function selectCamera() {
    // Выбор и переключение камеры
}
```

### 🎯 Особенности веб-интерфейса:

#### Модальное окно выбора камер:
1. **Загрузка камер**: Автоматическая загрузка списка доступных камер
2. **Визуальное представление**: Каждая камера отображается в виде карточки
3. **Информация о камере**: Название, путь устройства, форматы и разрешения
4. **Визуальное выделение**: Текущая и выбранная камеры выделяются цветом
5. **Интерактивность**: Клик по камере для выбора, подтверждение выбора

#### Структура карточки камеры:
```
┌─────────────────────────────────────────┐
│ 📷 USB 2.0 Camera: HD 720P Webcam      │
│ /dev/video0                            │
│                                         │
│ Форматы: YUYV, MJPG                    │
│                                         │
│ Разрешения: 640x360 640x480 1280x720   │
│                                         │
│ [Текущая камера]                        │
└─────────────────────────────────────────┘
```

#### Визуальные стили:
- **Текущая камера**: Синяя рамка и фон (#007bff)
- **Выбранная камера**: Зеленая рамка и фон (#28a745)
- **Обычная камера**: Серая рамка (#e8f4fd)
- **Анимации**: Плавные переходы и эффекты при наведении

### 📋 Что нового в интерфейсе

#### Новая кнопка "Камеры":
```
▶️ Запустить | ⏹️ Остановить | 📹 Камеры
```

#### Модальное окно:
```
┌─────────────────────────────────────────┐
│ 📹 Доступные камеры          [×]        │
├─────────────────────────────────────────┤
│                                         │
│ [📷 Камера 1]  ← Текущая                │
│ [📷 Камера 2]  ← Выбранная              │
│ [📷 Камера 3]                           │
│                                         │
├─────────────────────────────────────────┤
│ [Закрыть]          [Выбрать камеру]     │
└─────────────────────────────────────────┘
```

### 🚀 Преимущества веб-интерфейса:

1. **Удобство использования**: Визуальный выбор камер без перезапуска сервера
2. **Информативность**: Полная информация о возможностях каждой камеры
3. **Гибкость**: Возможность переключения между камерами в реальном времени
4. **Профессиональный вид**: Современный дизайн и анимации
5. **Адаптивность**: Работает на всех устройствах (мобильные, планшеты, ПК)

### 📁 Тестирование системы

#### Запуск системы с выбором камер
```bash
cd 004_code_flask_web_stream
python 04_flask_webcam_stream.py
```

#### Проверка API камер
```bash
# Получение списка камер
curl http://localhost:5000/api/cameras

# Выбор камеры
curl -X POST -H "Content-Type: application/json" \
     -d '{"device_path": "/dev/video2"}' \
     http://localhost:5000/api/cameras/select
```

#### Веб-интерфейс:
1. Открыть браузер: http://localhost:5000
2. Нажать кнопку "📹 Камеры"
3. Выбрать нужную камеру из списка
4. Нажать "Выбрать камеру"
5. Система автоматически переключится на выбранную камеру

### 📝 Пример использования

#### Запуск системы
```bash
python 04_flask_webcam_stream.py
# Система автоматически сканирует доступные камеры
# Веб-интерфейс доступен по http://localhost:5000
```

#### Работа с интерфейсом:
1. **Главная страница**: Просмотр текущего стрима и управление
2. **Кнопка "Камеры"**: Открытие модального окна с выбором
3. **Выбор камеры**: Клик по нужной камере
4. **Подтверждение**: Нажатие "Выбрать камеру"
5. **Автоматическое переключение**: Система перезапускает стрим с новой камерой

### 📊 Сравнение с предыдущими версиями

| Версия | Управление камерами | Интерфейс |
|--------|-------------------|-----------|
| 1.0 | Через конфигурацию | Только API |
| 2.0 | Через веб-интерфейс | Полноценный веб-интерфейс с выбором |

Веб-интерфейс для выбора камер обеспечивает удобное и интуитивное управление камерами!