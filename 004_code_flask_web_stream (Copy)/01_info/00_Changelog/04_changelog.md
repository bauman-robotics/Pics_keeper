# Changelog - Flask Webcam Stream v04

## Версия 04 - YAML Конфигурация (2026-01-31)

### 📋 Новые возможности

#### Поддержка YAML конфигурации
- **Гибкая настройка**: Все параметры вынесены в конфигурационный файл
- **Множество опций**: Камера, сервер, поток, пути, интервалы, advanced настройки
- **Командная строка**: Возможность указания пути к конфигурационному файлу

#### Конфигурационный файл `config.yaml`
```yaml
# Параметры сервера
server:
  host: "0.0.0.0"
  port: 5000
  debug: false
  threaded: true

# Параметры камеры
camera:
  device: 0
  backend: "auto"  # auto, v4l2, ffmpeg, direct
  width: 1280
  height: 720
  fps: 30
  jpeg_quality: 85
```

### 🔧 Технические улучшения

#### Гибкий выбор камеры
- **Авто-тестирование**: Автоматическое перебирание бэкендов
- **Ручной выбор**: Конкретный бэкенд по имени
- **Прямой доступ**: Возможность указания `/dev/video0` напрямую
- **Разрешение и FPS**: Настройка параметров камеры

#### Улучшенное управление
- **Конфигурационный парсер**: Загрузка YAML с обработкой ошибок
- **Валидация параметров**: Проверка существования файлов и папок
- **Гибкие настройки**: Возможность изменения всех параметров без перекомпиляции

#### Расширенный API статуса
```json
{
  "stream_active": true,
  "frame_count": 1234,
  "camera_connected": true,
  "config": {
    "device": 0,
    "backend": "auto",
    "resolution": "1280x720",
    "fps": 30,
    "jpeg_quality": 85
  }
}
```

### 🎯 Конфигурационные секции

#### Server (Сервер)
- `host` - Хост для прослушивания (по умолчанию: "0.0.0.0")
- `port` - Порт сервера (по умолчанию: 5000)
- `debug` - Режим отладки (по умолчанию: false)
- `threaded` - Многопоточный режим (по умолчанию: true)

#### Camera (Камера)
- `device` - Номер устройства камеры (0, 1, 2...)
- `backend` - Бэкенд: "auto", "v4l2", "ffmpeg", "direct"
- `direct_path` - Прямой путь к устройству для backend: "direct"
- `width/height` - Разрешение кадра
- `fps` - Кадров в секунду
- `jpeg_quality` - Качество JPEG (1-100)
- `test_backends` - Список бэкендов для авто-тестирования

#### Stream (Поток)
- `max_error_count` - Максимальное количество ошибок чтения
- `frame_log_interval` - Интервал логирования кадров
- `chunk_size` - Размер чанка для передачи

#### Paths (Пути)
- `templates_folder` - Папка с HTML шаблонами
- `log_file` - Файл логов (опционально)

#### Intervals (Интервалы)
- `status_update` - Интервал обновления статуса (мс)
- `error_retry_delay` - Задержка между попытками переподключения

#### Advanced (Дополнительно)
- `auto_restart` - Автоматический перезапуск при ошибках
- `verbose_logging` - Подробное логирование
- `enable_metrics` - Включить сбор метрик

### 🚀 Запуск с конфигурацией

#### Стандартный запуск
```bash
python 04_flask_webcam_stream.py
# Использует config.yaml из текущей директории
```

#### Запуск с указанием конфига
```bash
python 04_flask_webcam_stream.py --config /path/to/config.yaml
```

#### Примеры конфигураций

**Для Raspberry Pi:**
```yaml
camera:
  device: 0
  backend: "v4l2"
  width: 640
  height: 480
  fps: 15
  jpeg_quality: 70
```

**Для профессиональной камеры:**
```yaml
camera:
  device: 1
  backend: "ffmpeg"
  width: 1920
  height: 1080
  fps: 30
  jpeg_quality: 90
```

**Для прямого доступа:**
```yaml
camera:
  backend: "direct"
  direct_path: "/dev/video2"
  width: 1280
  height: 720
  fps: 25
```

### 📋 Список изменений

#### Добавлено
- YAML конфигурационный файл `config.yaml`
- Функция загрузки конфигурации с обработкой ошибок
- Поддержка командной строки для указания пути к конфигу
- Гибкая система выбора бэкендов камеры
- Настройка разрешения и FPS камеры
- Расширенный API статуса с информацией о конфигурации
- Автоматическое тестирование бэкендов по списку
- Валидация параметров конфигурации

#### Изменено
- Скрипт переписан для работы с конфигурацией
- Добавлен argparse для обработки аргументов командной строки
- Функция тестирования камер модифицирована под конфигурацию
- Генератор кадров использует параметры из конфига
- Flask приложение инициализируется с параметрами из конфига

#### Сохранено
- Все функции управления стримом
- Интерактивный веб-интерфейс
- Обратная совместимость с предыдущими версиями
- API маршруты и их функциональность

### 🎯 Цели реализации

1. **Гибкость настройки** - Возможность изменения всех параметров без перекомпиляции
2. **Удобство развертывания** - Разные конфигурации для разных сред
3. **Поддержка различных камер** - Автоматическое определение и настройка
4. **Профессиональное использование** - Поддержка сложных сценариев и настроек
5. **Документирование конфигурации** - Понятные параметры с комментариями

### 🔗 Совместимость версий

#### Версия 01 → 02 → 03 → 04
- **Версия 02**: HTML в шаблонах, улучшенный дизайн
- **Версия 03**: Добавлено управление стримом, интерактивный интерфейс
- **Версия 04**: YAML конфигурация, гибкая настройка параметров

#### Миграция на версию 04
1. Скопировать файл `04_flask_webcam_stream.py`
2. Создать папку `templates_04/` (или использовать существующую)
3. Скопировать HTML файлы в папку `templates_04/`
4. Настроить `config.yaml` под вашу систему
5. Запустить скрипт

**Важно**: Версия 04 полностью совместима с предыдущими версиями, добавлены только новые функции конфигурации.

### 📝 Примеры использования

#### Для разработки
```yaml
server:
  debug: true
  port: 8080
camera:
  device: 0
  backend: "auto"
  width: 640
  height: 480
```

#### Для продакшена
```yaml
server:
  debug: false
  host: "0.0.0.0"
  port: 5000
camera:
  device: 0
  backend: "v4l2"
  width: 1280
  height: 720
  jpeg_quality: 80
```

#### Для тестирования
```yaml
camera:
  backend: "direct"
  direct_path: "/dev/video1"
  test_backends: ["direct_video1"]
```

YAML конфигурация делает систему гибкой, удобной для настройки и профессионального использования!