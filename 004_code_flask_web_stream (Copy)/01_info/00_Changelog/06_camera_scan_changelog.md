# Changelog - Сканирование камер Flask Webcam Stream

## Версия со сканированием камер (2026-01-31)

### 📋 Новые возможности

#### Функция сканирования доступных камер
- **Автоматическое обнаружение**: Поиск всех доступных камер в системе
- **Параметры камер**: Определение поддерживаемых разрешений и FPS
- **Тестирование бэкендов**: Проверка V4L2, FFMPEG и Default бэкендов
- **Подробная информация**: Полная характеристика каждой найденной камеры

### 🔧 Технические улучшения

#### Новая функция: `scan_available_cameras()`
```python
def scan_available_cameras(self, max_devices: int = 10) -> List[Dict]:
    """Сканирование доступных камер и их параметров"""
```

#### Поддерживаемые разрешения для сканирования
```python
common_resolutions = [
    (640, 480),   # VGA
    (800, 600),   # SVGA
    (1024, 768),  # XGA
    (1280, 720),  # HD
    (1920, 1080), # Full HD
    (2560, 1440), # 2K
    (3840, 2160)  # 4K
]
```

#### Поддерживаемые FPS для сканирования
```python
common_fps = [5, 10, 15, 25, 30, 60]
```

#### Тестируемые бэкенды
- **V4L2**: `cv2.CAP_V4L2` - Video4Linux2
- **FFMPEG**: `cv2.CAP_FFMPEG` - FFMPEG бэкенд
- **Default**: `None` - Стандартный бэкенд OpenCV

### 📋 Что логируется при сканировании

#### Общая информация о сканировании
```
======================================================================
🔍 СКАНИРОВАНИЕ ДОСТУПНЫХ КАМЕР
======================================================================
```

#### Для каждого устройства
```
🔍 Проверка устройства /dev/video0
   ✅ V4L2 - Устройство доступно
   📐 Базовое разрешение: 1280x720
   📊 Базовый FPS: 30
   📋 Поддерживаемые разрешения: 640x480, 800x600, 1280x720, 1920x1080
   📋 Поддерживаемые FPS: 5, 10, 15, 25, 30
```

#### Сводка по найденным камерам
```
📊 Найдено камер: 2
📋 СВОДКА ПО КАМЕРАМ:
   1. /dev/video0 (V4L2)
      Разрешение: 1280x720
      FPS: 30
      Поддерживаемые разрешения: 640x480, 800x600, 1280x720, 1920x1080
      Поддерживаемые FPS: 5, 10, 15, 25, 30
   2. /dev/video2 (FFMPEG)
      Разрешение: 1920x1080
      FPS: 25
      Поддерживаемые разрешения: 640x480, 1280x720, 1920x1080
      Поддерживаемые FPS: 5, 10, 15, 25
======================================================================
```

### 🎯 Особенности сканирования

#### Методика тестирования
1. **Поиск устройств**: Проверка устройств от `/dev/video0` до `/dev/video9`
2. **Тестирование бэкендов**: Для каждого устройства пробуются все доступные бэкенды
3. **Определение базовых параметров**: Получение текущих разрешения и FPS
4. **Тестирование разрешений**: Проверка поддержки стандартных разрешений
5. **Тестирование FPS**: Проверка поддержки стандартных значений FPS

#### Критерии поддержки параметров
- **Разрешение**: Установленное значение должно соответствовать полученному
- **FPS**: Допускается погрешность ±2 FPS от целевого значения

#### Обработка ошибок
- **Недоступное устройство**: Логируется как "❌ Устройство недоступно"
- **Ошибка бэкенда**: Логируется с указанием типа ошибки
- **Ошибка параметров**: Логируется как предупреждение

### 📁 Интеграция в систему

#### Вызов функции сканирования
```python
# В функции main() после загрузки конфигурации
available_cameras = logger.scan_available_cameras()
```

#### Возвращаемые данные
```python
camera_info = {
    'device_id': 0,                    # Номер устройства
    'backend': 'V4L2',                 # Используемый бэкенд
    'base_resolution': '1280x720',     # Базовое разрешение
    'base_fps': 30,                    # Базовый FPS
    'supported_resolutions': ['640x480', '1280x720'],  # Поддерживаемые разрешения
    'supported_fps': ['5', '10', '15', '30']           # Поддерживаемые FPS
}
```

### 🚀 Преимущества сканирования

1. **Диагностика системы**: Быстрое определение доступных камер
2. **Выбор оптимальных параметров**: Информация для настройки конфигурации
3. **Решение проблем**: Помощь в настройке камер с нестандартными параметрами
4. **Документирование**: Полная информация о возможностях каждой камеры
5. **Автоматизация**: Возможность автоматического выбора наилучшей камеры

### 📝 Пример использования

#### Запуск с сканированием камер
```bash
python 04_flask_webcam_stream.py
# Автоматически выполнит сканирование перед запуском
```

#### Анализ логов
```bash
# Поиск информации о камерах
grep "🔍 СКАНИРОВАНИЕ ДОСТУПНЫХ КАМЕР" 002_logs/flask_stream_*.log

# Поиск конкретной камеры
grep "/dev/video0" 002_logs/flask_stream_*.log

# Анализ поддерживаемых разрешений
grep "Поддерживаемые разрешения" 002_logs/flask_stream_*.log
```

### 🔧 Настройка сканирования

#### Изменение количества проверяемых устройств
```python
# В вызове функции
available_cameras = logger.scan_available_cameras(max_devices=20)
```

#### Добавление новых разрешений для тестирования
```python
# В функции scan_available_cameras()
custom_resolutions = [
    (320, 240), (640, 480), (1280, 720), (1920, 1080)
]
```

#### Добавление новых FPS для тестирования
```python
# В функции scan_available_cameras()
custom_fps = [1, 5, 10, 15, 20, 25, 30, 60]
```

Сканирование камер делает систему более прозрачной и удобной для настройки!